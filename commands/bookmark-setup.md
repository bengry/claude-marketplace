---
description: Install claude-bookmark CLI to ~/.local/bin and shell completions for fish/bash/zsh
argument-hint: "[fish|bash|zsh]"
allowed-tools: Bash(bash:*)
---

!`bash ${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh $ARGUMENTS`

Print the bash output above verbatim inside a fenced code block, with no extra commentary or `<tldr>`.
